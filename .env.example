# Variables para Docker Compose (PostgreSQL local)
DB_USER=postgres
DB_PASSWORD=tu_contraseña_segura
DB_NAME=classboard_db

# Consulta server/.env.example y client/.env.example para las variables del backend y frontend
